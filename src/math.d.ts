import { Basic } from './basic';

export declare class Math extends Basic {
  constructor();

  calculateAverageValuesInAnArray(array: number[]): number;

  medianOfAnArray(array: number[]): number | null;

  modeOfAnArray(array: number[]): number | null;
}
